.upload-container {
    display:flex;
    flex-wrap:wrap;
    justify-content:center;/* 水平居中 */
    padding:0 10px; /* 左右留出一些间距 */
    width:100%;/* 适应父容器的宽度 */
    box-sizing:border-box; /* 边距不会被宽度所影响 */
    text-align:center;/* 文字水平居中，对于图片也适用 */
   }
   
   .upload-container img {
    width:calc(33.33% - 20px);/* 图片占据 1/3 宽度，并考虑到间距 */
    margin:30px;/* 图片之间的间距 */
    box-sizing:border-box; /* 确保内边距和边框不会增加宽度 */
   }
   
   /* 为了处理最后一行图片不满的情况 */
   @media (max-width:768px) {
    .upload-container img {
      width:calc(50% - 20px);/* 图片占据一半宽度，并考虑到间距 */
    }
   }