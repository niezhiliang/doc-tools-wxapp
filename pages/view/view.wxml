<view>
    文件详情
    <block wx:for="{{fileList}}" wx:key="item" wx:for-item="item" wx:for-index="i">
    <view>文件地址：{{item.url}}</view>
    <view>文件key：{{item.urlKey}}</view>
  </block>

    <van-button icon="photo" type="primary" size="large" bind:click="onConvert">立即转换</van-button>
</view>