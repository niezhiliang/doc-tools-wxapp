# 免费验证版验收

本版主流程：首页 → 转换工作页（选文件、图片排序、上传、提交）→ 当前任务结果。
转换记录为独立原生 Tab，处理中自动刷新。当前不包含广告或收费限制。

## 本地检查

运行 `node doc-tools-wxapp/tests/flow.cjs`。
覆盖部分图片上传失败不得提交、成功部分重试复用、图片顺序、任务 ID 跳转、轮询生命周期、页面文件完整性。
这些模拟检查不代替微信开发者工具和真机验收。

## 微信开发者工具 / 真机待验收

- 直接导入小程序目录，无需安装依赖或构建 npm；配置可访问的后端地址。当前 app.js 仍为本机开发地址。
- 后端与前端一起更新：转换提交现在返回 data.recordId，任务详情不再缓存处理中状态。
- PDF 转 Word、Word 转 PDF、图片转 PDF、PDF 转图片各完成一次真实转换。
- 文件名含中文、空格、特殊字符时能选择、上传、导出。
- 选择文件取消、超大小、断网、登录失败能理解原因并恢复。
- 三张图片排序、删除、补充后，结果页顺序与屏幕一致；任一图片上传失败不得漏图生成。
- 处理期间退出页面后停止轮询，从转换记录回来能查询结果。
- 安卓和 iOS 分别验收文档发送、预览菜单和图片保存；相册拒绝授权后有明确提示。
- 真机检查长文件名、小屏幕和大字号可读性。

## 运营前仍需确认

- 统一文件保留策略。当前转换记录查询近 3 天，旧通知文案及服务端清理实现尚未统一，本版不承诺准确删除时间。
- 以真实样本文档检查转换排版质量，尤其扫描版 PDF。
- 尚未接入真实运营埋点；上线观察需补齐选文件、提交、转换成功、导出成功四个事件，不记录文件内容。
- 后续广告解锁需服务端校验观看凭证并限制结果文件访问；仅隐藏前端按钮不足以形成下载门槛。

## 任务队列版本补充

实现与部署要求见项目根目录 TASK_QUEUE.md。新增验收：

- 同时提交不同用户任务，只允许一个转换，其余排队；同用户重复点击打开已有任务。
- 等待达到 10 单时，下一位看到队列已满，已上传文件仍可稍后提交。
- 结果页区分排队与转换，显示前方任务数；每 5 秒更新，离开后停止请求。
- 停止并重启单实例：已排队任务继续，原转换中任务显示中断说明；不能用两个实例重叠测试。
- 超过 10MB 单文件、超过 20MB 图片总量或 50 页 PDF 显示明确失败说明。
- 上线前执行扩展 source_file 字段的增量迁移，已有库不要执行空库初始化 SQL，参考 db/README.md 选择增量脚本。
